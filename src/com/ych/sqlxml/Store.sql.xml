<?xml version="1.0" encoding="UTF-8"?>
<sqlGroup namespace="Store">
	
	<!-- 动态SQL处理 -->
	<sql id="pager">
		<![CDATA[ 
			from(
				select `name`,c_time from store where 1=1
				<% if(!isEmpty(storeName)){ %>
				 AND name like #'%$storeName$%'#
				<% } %>
			)t order by t.c_time
		]]>
	</sql>
	
</sqlGroup>